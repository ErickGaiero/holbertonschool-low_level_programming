#include <stdio.h>

/**
 * main - It's the principal function
 * Return: Return zero
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

return (0);
}
