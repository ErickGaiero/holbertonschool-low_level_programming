#include "main.h"

/**
 * _isupper - Print 1 and 0 to upper or another
 * @c: Character
 * Return: return 1 if is upper return 0 if is another
 */

int _isupper(int c)
{

	if (c >= 'A' && c <= 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
