#include "main.h"

/**
 * _abs - Print the absolute value of a number
 * @r: Number ingresed
 * Return: -r if is negative, r if another case
 */

int _abs(int r)
{

	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}

}
